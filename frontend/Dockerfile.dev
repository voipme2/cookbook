FROM node:18-alpine

WORKDIR /app

# Install dependencies for development
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Start development server without Turbopack for better compatibility
CMD ["npm", "run", "dev", "--", "--no-turbo"] 